% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/current_double_dispatch_solution.R
\name{PH}
\alias{PH}
\alias{PH_pointcloud}
\alias{PH_raster}
\title{Classes for persistent homology specifications}
\usage{
PH(
  filtration = "vietoris_rips",
  engine = "ripserr",
  library = NA_character_,
  max_dimension = 1
)

PH_pointcloud(
  filtration = "vietoris_rips",
  engine = "ripserr",
  library = NA_character_,
  max_dimension = 1,
  max_diameter = NA_real_
)

PH_raster(
  filtration = "vietoris_rips",
  engine = "ripserr",
  library = NA_character_,
  max_dimension = 1,
  max_scale = NA_real_,
  sublevel = TRUE
)
}
\arguments{
\item{filtration}{filtration used in persistence calculation}

\item{engine}{back-end engine used in persistence calculation}

\item{library}{c++ library used for TDA engine}

\item{max_dimension}{maximum homological dimension to compute persistence}

\item{max_diameter}{maximum threshold for rips filtration (point clouds)}

\item{max_scale}{maximum threshold for rips filtration (rasters)}

\item{sublevel}{specifies sublevel or superlevel filtration}
}
\value{
S7 object storing user specification for calculating persistence
}
\description{
These class define the parameters needed for persistence
calculations and allow for reuse of specification for different data sets.
The parent class, \code{PH}, stores core attributes that are common among all
persistence calculations. The point cloud subclass is used to handle
distance matrices and point cloud arrays. The raster subclass is used
to handle grid data encoded as matrices or arrays.
}
\examples{

pc <- PH_pointcloud(filtration = "vietoris_rips",
              engine = "ripserr",
              max_dimension = 1,
              max_diameter = 1)

r <- PH_raster(filtration = "cubical",
          engine = "TDA",
          library = "GUDHI",
          max_dimension = 2,
          max_scale = 10,
          sublevel = TRUE)
}
